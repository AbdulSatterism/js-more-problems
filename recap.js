/*
function maximumElement(numbers) {
    let max = numbers[0];
    for (let i = 0; i < numbers.length; i++) {
        const element = numbers[i];
        if (element > max) {
            max = element;
        }
    }
    return max;
}

const array = [21, 54, 21, 32, -5, 15];
const result = maximumElement(array)
console.log(result)
*/
